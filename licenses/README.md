# Third-party components

The source code in this project is MIT-licensed. Model weights are downloaded
separately from the official OpenCV Zoo repository at the commit in
`models/manifest.json`; their original license texts are included here and copied
into the built app's resources.

- YuNet detection model: MIT, Shiqi Yu; see `YuNet-MIT.txt`.
- SFace recognition model: Apache License 2.0; see `SFace-Apache-2.0.txt`.
- OpenCV: supplied by Homebrew as a separate dependency. Retain its notices and
  any transitive dependency notices if you later redistribute bundled binaries.

No model is trained or modified by this project. The SHA-256 values match the
upstream Git LFS object identifiers for the pinned model files.
